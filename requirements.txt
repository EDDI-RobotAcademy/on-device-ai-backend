# =============================================================================
# On-Device AI Backend — 전체 과정 의존성
#
# 원칙: 실습 도중에 "pip install" 로 멈추지 않는다.
#       과정 전체(데이터 → 품질 → 모델 → 최적화 → 운영 → AWS)에서 필요한
#       라이브러리를 시작할 때 한 번에 전부 설치한다.
#
# 설치:
#   bash scripts/setup.sh
# 또는:
#   .venv/bin/python -m pip install -r requirements.txt
#   .venv/bin/python -m scripts.preflight
# =============================================================================

# --- Interfaces: HTTP -------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
python-multipart>=0.0.9

# --- Infrastructure: 데이터 분석 / 측정 --------------------------------------
numpy>=1.26
pandas>=2.2
pyarrow>=17.0
scipy>=1.13
scikit-learn>=1.5
pillow>=10.4

# --- Infrastructure: 모델 (PyTorch) ------------------------------------------
torch>=2.5
torchvision>=0.20

# --- Infrastructure: 최적화 / 온디바이스 런타임 -------------------------------
onnx>=1.17
onnxscript>=0.2          # torch.onnx.export 가 요구한다
onnxruntime>=1.19
tensorflow>=2.18          # TFLite 변환기 (converter)
ai-edge-litert>=1.0       # TFLite 런타임 (interpreter)

# --- Infrastructure: 영속화 --------------------------------------------------
SQLAlchemy>=2.0

# --- Infrastructure: AWS -----------------------------------------------------
boto3>=1.35
botocore>=1.35

# --- Observability -----------------------------------------------------------
structlog>=24.4
psutil>=6.0              # 실습 4-13 — 추론 중 CPU·메모리 실측

# --- Test --------------------------------------------------------------------
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
httpx>=0.27
moto[s3,dynamodb,sagemaker]>=5.0
